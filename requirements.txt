# Core ML / fine-tuning
# Keep a ceiling so `pip install -U -r requirements.txt` does not replace the
# CUDA-matched Torch wheel provided by GPU images. The Vast image used here has
# torchvision pinned to torch 2.12.0.
torch>=2.12.0,<2.12.1
transformers>=5.9.0
peft>=0.19.1
trl>=0.12.0
bitsandbytes>=0.49.2
accelerate>=1.13.0

# Data handling
datasets>=2.18.0
huggingface_hub>=1.17.0

# Evaluation
rouge-score>=0.1.2
openai>=1.40.0
# Optional: better semantic similarity metric
# sentence-transformers>=3.0.0   # for BERTScore with multilingual-mpnet-base-v2

# Utilities
Pillow>=10.0.0
numpy>=1.26.0
tqdm>=4.66.0
